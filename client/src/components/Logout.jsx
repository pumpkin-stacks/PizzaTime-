import React, { useState } from 'react';

const Logout = (props) => {
    return (
        <div>
            <h1>You have successfully logged out!</h1>
        </div>
    );
};

export default Logout;